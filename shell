(exit 3) | (exit 4) | (exit 5)
# 終了ステータスを　３　４　５　に設定

 echo ${pipestatus[@]}　　　#　英小文字でシェル変数を指定すると何も表示されない
 
 echo ${PIPESTATUS[@]}　　#　大文字でシェル変数を指定すると　３　４　５　と表示される
 3 4 5
 
 #　終了ステータスは　コマンド実行をすると　消えるので　(exit 3) | (exit 4) | (exit 5)　を直前に実行
